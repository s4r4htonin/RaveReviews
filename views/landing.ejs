<!DOCTYPE html>
<html>

<head>
    <title>RaveReviews</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/landing.css">
    <script src="https://kit.fontawesome.com/3f292a0c23.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript"
        async></script> <!--Makes more cross-browser compatible-->
</head>

<body>

    <div class="container">
        <% if(error && error.length > 0){ %> <!--if error and message, show flash error message -->
        <div class="alert alert-danger" role="alert">
            <%= error %>
        </div>
        <% } %>
        <% if(success && success.length > 0){ %>
        <div class="alert alert-success" role="alert"> <!--if success and message, show flash success message -->
            <%= success %>
        </div>
        <% } %>
    </div>

    <div id="landing-header">
        <h1><i class="fas fa-music"></i> Welcome to RaveReviews!</h1>
        <a href="/festivals" class="btn btn-lg btn-primary">View All Festivals</a>
    </div>

    <ul class="slideshow"> <!--Slideshow of images on landing page -->
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
    </ul>

    <%- include("partials/footer") %>